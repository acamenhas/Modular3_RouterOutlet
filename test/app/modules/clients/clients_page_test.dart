import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:modular3/app/modules/clients/pages/clients_list_page.dart';

void main() {
  testWidgets('ClientsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ClientsPage(title: 'Clients')));
    //  final titleFinder = find.text('Clients');
    //  expect(titleFinder, findsOneWidget);
  });
}
